refactor points+ScalarField->points (relaxation) block
refactor points ---(MST)--> points (ordering) block
refactor Logger for more convinient (iostream or cstdio style) text input
add more standard demos
add packing block networks into single block
